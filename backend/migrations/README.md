# Alembic Migrations

This directory is reserved for Alembic migration scripts.

Initialize later with:

- `alembic init migrations` (if you choose default layout), or
- keep this directory and wire `alembic.ini` to it.
