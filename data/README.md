# Data Directory

Use this folder for local seed files, source snapshots, and registry imports used in development/testing.

- `seed/`: deterministic local seed data
- `gold/`: labeled benchmarking sets for matching precision tests
